apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "diya.fullname" . }}-test-connection"
  labels:
    {{- include "diya.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "diya.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
