apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-ingestion-service.fullname" . }}-test-connection"
  labels:
    {{- include "data-ingestion-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-ingestion-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
