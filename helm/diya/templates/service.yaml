apiVersion: v1
kind: Service
metadata:
  name: {{ include "diya.fullname" . }}
  labels:
    {{- include "diya.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "diya.selectorLabels" . | nindent 4 }}
