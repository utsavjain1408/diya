apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frontend-gui.fullname" . }}-test-connection"
  labels:
    {{- include "frontend-gui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "frontend-gui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
